services:
  - type: worker
    name: medical-insight-worker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python worker.py
    envVars:
      - key: GEMINI_API_KEY
      - key: RESEND_API_KEY
      - key: DATABASE_URL
      - key: ENTREZ_EMAIL